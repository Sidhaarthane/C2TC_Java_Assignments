package Assignment4;


public interface Airfare {
    // Declare the display method as per the table, although the name is different in the table
    // It's referred to as calculateAmount() in the attributes table.
    // We'll stick to the method name and return type provided in the table.
    double calculateAmount();
}